#queues and stacks can be implemeted by built in lists.

#stacks
#useful to keep track of recursive comp.
#s.append(x) to insert at last of a list
#s.pop() to remove from the last of the list. builtin fn.


#queues
#FIFO. useful for breadth first search
#addq(q,x) --- add x to the rear of the q
#remove(q) ---- remove element at head of q.
#Python lists, left is rear, right is front. pop() can be used to remove at front
#add(q,x) is q.insert(0, x)
#q.pop()
